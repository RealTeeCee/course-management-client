// import { createSelector } from "@reduxjs/toolkit";

// const selectLoginReducer = (state) => state.login; //store in rootReducer

// export const selectLoginData = createSelector(
//   [selectLoginReducer],
//   (loginSlice) => {
//     return loginSlice.data;
//   }
// );

// export const selectLoginIsFetching = createSelector(
//   [selectLoginReducer],
//   (loginSlice) => loginSlice.isFetching
// );

// export const selectLoginIsSuccess = createSelector(
//   [selectLoginReducer],
//   (loginSlice) => loginSlice.isLoginSuccess
// );

// export const selectLoginFailed = createSelector(
//   [selectLoginReducer],
//   (loginSlice) => loginSlice.error
// );

// export const selectRefreshTokenIsSuccess = createSelector(
//   [selectLoginReducer],
//   (refreshSlice) => refreshSlice.isRefreshSuccess
// );
